# Contributing

Open an issue before changing scientific equations, coefficients, QA rules,
scaling, nodata handling, TIMESAT settings, temporal sampling, or output names.
Such changes require scientific review and regression evidence.

Keep local configuration, credentials, data, outputs, logs, and binaries out of
Git. Run `pytest -q`, Python syntax compilation, Ruff, ShellCheck, and JSON
validation before proposing a change.
